# Environment Configuration
NODE_ENV=development

# Database
MONGODB_URI=mongodb://localhost:27017/ambassadorhub

# Server
PORT=3001

# Authentication (to be implemented)
JWT_SECRET=your-secret-key-change-this-in-production
JWT_EXPIRY=7d

# CORS
CORS_ORIGIN=http://localhost:3000

# Stellar Soroban (for StreamLink integration)
# Network passphrase for testnet
STELLAR_NETWORK="Test SDF Network ; September 2015"
# Contract ID (use your deployed contract id or keep mock for dev)
STELLAR_CONTRACT_ID=mock-contract-id
# RPC URL for Soroban
STELLAR_RPC_URL=https://soroban-testnet.stellar.org

# Asset configuration
ASSET_CODE=USDC
# Optional issuer for non-native assets
ASSET_ISSUER=
